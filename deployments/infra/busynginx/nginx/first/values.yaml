image:
  pullPolicy: Always

resources:
  limits:
    memory: 50.0Mi
    cpu: 1.0
  requests:
    memory: 50.0Mi
    cpu: 1.0

fullnameOverride: nginx-test-7
namespaceOverride: infra

commonLabels: 
  app.kubernetes.io/name: nginx-test-7  

nodeSelector:
  app.kubernetes.io/name: nginx-test-7

# podLabels:
#   app.kubernetes.io/name: nginx-test-7
